1：有必要时重新设计数据库，可以对数据库进行删除

## 任务1：工件触发-增加处理工件

​	界面上的流水号取自 **workpiece_table** 表 id,

​	在实时模式下，默认使用 **workpiece_table** 最新的一条数据，

​	workpiece_table表为空时，增加一条测试数据，在 seed.py 中完成。

​	在主菜单中 数据菜单 中增加 手动添加数据选项，

​	在/dialogs 文件夹中增加 数据添加窗口，用于向后端增加数据

​	server 不进行数据库通讯，将刀具信息等移交到http_prod



## 任务2：采集触发逻辑

​	在急停，停止之间增加 **采集** 按钮(非全自动模式可用), 当 状态信息为状态 [设备准备就绪时,这些准备就绪(重新识别)],采集按钮可以进行点击 , 点击后 在 record_table 表生成一条记录(record_table 增加外键 与 workpiece_table id 表进行关联 ),然后通过访b问配置新建文件夹(暂时在**/SaveData**文件夹.根据 record_table  id 创建), 在task_tabel 增加一条采集任务(增加 外键 到 record_table  id)

​	定义一些常量类(控制,任务,异常,状态...)

​	然后监听任务状态,如果状态为 完成状态.则向server 发送控制代码,暂时使用虚拟值,(实际值从r_algorithm_data获取.)

​	控制 代码改为 EX, EY, EX (移动到的位置),R(主轴转速),V(移动速度)  (条指令),

​	状态为执行准备就绪时 开始按钮可用,,

当点击开始时,添加一条 执行任务(task_tabel),并且通过监控该条数据更新执行状态(增加字段(json)来更新执行状态)

​	控制指令也视为task,向 task_tabel 进行添加.



## 任务3：执行DEMO

​	增加一个虚假的执行层,通过 数据库与 http_prod 进行交互,通过虚假的状态,采集,算法,控制,执行,用于演示 http_prod