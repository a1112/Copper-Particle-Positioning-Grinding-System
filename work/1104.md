## 任务1 根据流水号新建文件夹
在 D:\SaveData 中新建 record 文件夹，当采集点击 新的record_table数据增加时，新建 record/{record.id} 文件夹，当采集任务完成时，检查record/{record.id} 文件夹，如果无数据，将current/中的 tif,png,json 拷贝过去，（并且新建线程保存obj，使用rts_点云数据（rts_X1，rts_Y1,rts_Z1）,使用 balsam.exe 转换为meshes），在拷贝完成时，刷新图像数据，移除定时刷新以及双缓冲

## 任务2：
修改 demo_task_runner.py 当检测到 新的hardware_task_queue 采集任务时，10s后设置完成
