## 任务1：view/Task/TaskView.qml

​	该界面与任务队列界面基本一致（共用相同的数据，将statePageCore 中 Task 相关数据移动到/datas/TaskDatas）.任务流程按照顺序包含 采集任务（3D图像采集->算法处理），执行任务（路径执行），校验任务（采集图像，验证），数据来源从HardwareTaskQueue 数据库获取，未匹配到数据则为（未下发状态），匹配到后则根据 status 变化状态（排队中，执行中，完成...）

​	HardwareTaskQueue  根据 record_id 来查询，

​	currentRecordId 应该保存在 CoreCurrent.qml 中，（变化时触发刷新）。



