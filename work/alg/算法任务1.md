任务说明：
    描述通过3D结构光相机+3轴机床 进行铜板粒子切削（切掉铜板表面突起的粒子）
    新建测试工程，读取 D:\SaveData\current\  src开头 的点云，法线... 数据自动识别出铜板的位置，基础表面高度，给出> n 毫米粒子的数据，自动识别出气缸（夹具，铜板边缘高于铜板，用于固定）。对刀具的移动进行路径规划
        路径规划要参考气缸避让（控制气缸抬起（90度旋转）），刀具直径（不能z 方向切削），预留的表面高度，考虑到扭矩设计不同密度下的切削深度，速度，非切削下的移动速度，只允许单 X/Y 方向切削 的路径设计。

生成图像检测数据，类似rts,路径数据，路径绘制模拟（opencv 单步模拟）
粒子可能弥散也可能密集。