任务说明：
    描述通过3D结构光相机+3轴机床 进行铜板粒子切削（切掉铜板表面突起的粒子）
    新建测试工程，读取 D:\SaveData\current\  src开头 的点云，法线... 数据自动识别出铜板的位置，基础表面高度，给出> n 毫米粒子的数据，自动识别出气缸（夹具，铜板边缘高于铜板，用于固定）。对刀具的移动进行路径规划
        路径规划要参考气缸避让（控制气缸抬起（90度旋转）），刀具直径（不能z 方向切削），预留的表面高度，考虑到扭矩设计不同密度下的切削深度，速度，非切削下的移动速度，只允许单 X/Y 方向切削 的路径设计。

生成图像检测数据，类似rts,路径数据，路径绘制模拟（opencv 单步模拟）
粒子可能弥散也可能密集。
多文件分模块编写


更改1：
› configs/calibration/src_IMG_Color.xml 用labelimg 标注了夹具位置，重新编辑找板逻辑，板子是一个整体（矩形规格），平台距
  离>1780,所以<1780 的中间最大轮廓为板子和夹具，夹具通过上面的xml标注了，夹具范围基本<1775.
  1:找到夹具+板子（最大轮廓）
  2：夹具以外的区域拟合出板子的形状（矩形 作业范围），计算出板子基面的高度，板子边部60mm一般较矮，尽量拟合出一个基础面 z 值图 （避免翘起或者基础面倾斜（板子一边厚一边薄））
  3：将夹具（xml 区域中<1750的最大轮廓集合） 绘制为红色，板子区域绘制为蓝色保存）
  4，绘制 HSV（夹具特殊颜色，板外区域黑色）保存
  5，绘制粒子区域 hsv,设定保留平面高度（假设2mm）板子设置为底色，板外黑色，夹具...