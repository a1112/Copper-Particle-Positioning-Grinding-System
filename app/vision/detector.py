from typing import Optional, Tuple

class Detector:
    def locate(self, frame) -> Optional[Tuple[float, float, float, float]]:
        # placeholder: x,y,theta,score
        return (0.0, 0.0, 0.0, 0.8)
